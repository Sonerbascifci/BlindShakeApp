name: blind_shake
description: "BlindShake - Anonim eşleşme ve sohbet uygulaması"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.6.0
  firebase_database: ^11.2.0
  firebase_storage: ^12.3.7
  firebase_messaging: ^15.1.6
  
  # State Management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  
  # Navigation
  go_router: ^14.6.2
  
  # Location & Sensors
  geolocator: ^13.0.2
  sensors_plus: ^6.1.1
  geoflutterfire_plus: ^0.0.29
  
  # UI
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.2
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  
  # Auth
  google_sign_in: ^6.2.2
  
  # Utils
  intl: ^0.19.0
  shared_preferences: ^2.3.4
  permission_handler: ^11.3.1
  uuid: ^4.5.1
  json_annotation: ^4.9.0
  freezed_annotation: ^2.4.4
  cloud_functions: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  riverpod_generator: ^2.6.3
  build_runner: ^2.4.13
  mockito: ^5.4.4
  fake_cloud_firestore: ^3.0.5
  json_serializable: ^6.8.0
  freezed: ^2.5.7

flutter:
  uses-material-design: true
